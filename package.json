{
  "private": true,
  "dependencies": {
    "babel": "^5.8.23",
    "chai": "^3.3.0",
    "jscs": "^2.2.1",
    "mocha": "^2.3.3",
    "object-assign": "^4.0.1"
  },
  "scripts": {
    "compile": "babel src --out-dir dist",
    "start": "babel src  --watch --out-dir dist --source-maps",
    "test": "npm run lint && mocha test/*",
    "lint": "jscs ."
  }
}
